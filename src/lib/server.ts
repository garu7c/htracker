/*import { createServerSupabaseClient } from '@/lib/server';
import { cookies } from 'next/headers';
import { Database } from './database.types'; // Importa tus tipos de DB

export function createServerSupabaseClient() {
  // `createServerComponentClient` es la función recomendada para entornos de servidor
  // que usan la API de Next.js `cookies()`.
  return createServerComponentClient<Database>({
    cookies,
  });
}
*/